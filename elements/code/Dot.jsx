// The Dot geometry convenience.
<Svg width={px(480)} height={px(300)}>
  <Box padding={px(30)}>
    <Dot
      width={px(30)}
      height={px(30)}
      fill={blue}
      stroke={blue}
      stroke_width={px(3)}
    />
  </Box>
</Svg>
