# Set Theory

*Category*: geometry

Two smaller elliptical regions lie inside the larger set A, with independently positioned labels.

Ported from the old Gum gallery.

The old frame-shape option is replaced by sibling Ellipse and Text elements in a Group. Explicit sizes and anchors make the nesting easy to adjust.

See [Ellipse](../../elements/text/Ellipse.md) and [the runnable source](../code/set_theory.jsx).
