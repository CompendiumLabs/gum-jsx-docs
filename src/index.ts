// @gum-jsx/docs: the gum.jsx documentation and gallery examples
//
// The content lives in docs/ and gala/ as plain files; this module exports the
// directories holding them (dirs.ts) and the loaders that index them into pages
// (meta.ts). The raw files are also reachable through the ./docs/* and ./gala/*
// subpath exports.

export * from './dirs'
export * from './meta'
